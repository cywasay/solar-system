import ExploreLoader from '@/components/explore/ExploreLoader';

/** Immediate route-transition feedback while the Explore page is being requested. */
export default function Loading() {
  return <ExploreLoader />;
}
